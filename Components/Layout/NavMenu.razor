<div class="top-row ps-3 navbar navbar-dark bg-primary d-flex justify-content-between align-items-center">
    <a class="navbar-brand fw-bold m-0" href="">🛡️ ZeloApp</a>
    <button title="Navigation menu" class="navbar-toggler me-2 border-0" @onclick="ToggleNavMenu">
        <span class="navbar-toggler-icon"></span>
    </button>
</div>

<div class="@NavMenuCssClass nav-scrollable bg-dark text-white" @onclick="ToggleNavMenu">
    <nav class="flex-column py-2">
        <div class="px-3 text-white-50 small fw-bold my-2">SUPER ADMIN</div>
        <div class="nav-item px-3">
            <NavLink class="nav-link text-white" href="" Match="NavLinkMatch.All">
                🏠 Painel Geral
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link text-white" href="superadmin/escolas">
                🏫 Gestão de Escolas
            </NavLink>
        </div>

        <hr class="border-secondary my-2 mx-3" />

        <div class="px-3 text-white-50 small fw-bold mb-2">VISÃO DA FAMÍLIA</div>
        <div class="nav-item px-3">
            <NavLink class="nav-link text-white" href="portal/pai">
                📱 Portal do Pai/Mãe
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}